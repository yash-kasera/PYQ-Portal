/* Solutions — Object Oriented Programming Using Java */
window.SOLUTIONS = window.SOLUTIONS || {};
(function () {
  var S = {};

  S["oop-vs-pop"] = [
"### Object oriented vs procedure oriented programming",
"",
"#### Procedure oriented programming (POP)",
"",
"A program is viewed as a **sequence of instructions** grouped into functions. Data moves openly between functions, and the emphasis is on *what happens to the data* rather than on the data itself. Languages: C, FORTRAN, Pascal, COBOL.",
"",
"#### Object oriented programming (OOP)",
"",
"A program is viewed as a **collection of objects** that communicate by sending messages. Each object bundles data (attributes) with the operations (methods) that act on it, and the internal data is hidden from outside. Languages: Java, C++, Python, C#, Smalltalk.",
"",
"---",
"",
"#### Comparison",
"",
"| Point | Procedure Oriented | Object Oriented |",
"|---|---|---|",
"| Basic unit | Function / procedure | Object (instance of a class) |",
"| Program divided into | Functions | Objects |",
"| Emphasis | On procedures / algorithms | On data |",
"| Data movement | Data moves freely between functions | Data is hidden inside objects |",
"| Data security | Poor — global data is exposed | Strong — encapsulation and access modifiers |",
"| Approach | Top-down | Bottom-up |",
"| Access control | No access specifiers | public, private, protected, default |",
"| Overloading | Not supported | Function, method and operator overloading |",
"| Inheritance | Not supported | Supported |",
"| Code reuse | Copy-paste, limited | Inheritance, composition, interfaces |",
"| Real-world modelling | Poor | Natural |",
"| Suitable for | Small programs | Large, complex, evolving systems |",
"| Example | C | Java, C++ |",
"",
"---",
"",
"#### Merits of OOP",
"",
"1. **Reusability** — inheritance lets existing, tested classes be extended instead of rewritten.",
"2. **Data security through encapsulation** — data is private and reachable only through controlled methods, so it cannot be corrupted accidentally.",
"3. **Modularity** — each class is a self-contained unit that can be developed and tested independently.",
"4. **Maintainability** — a change inside a class does not affect its users as long as the interface is unchanged.",
"5. **Polymorphism** — the same interface serves many implementations, so new types can be added without changing the existing code.",
"6. **Natural modelling** — objects correspond to real-world entities, so the design is easier to understand.",
"7. **Easier team development** — different developers can work on different classes concurrently.",
"8. **Reduced complexity** through abstraction — the user sees only what is essential.",
"",
"#### Demerits of OOP",
"",
"1. **Larger program size** — OOP code is typically 30–40 % longer in lines than equivalent procedural code.",
"2. **Slower execution** — dynamic dispatch, object creation and garbage collection add overhead.",
"3. **Steeper learning curve** — the concepts (abstraction, polymorphism, dynamic binding) take time to master.",
"4. **Over-engineering** — small problems can be made unnecessarily complicated by forcing them into class hierarchies.",
"5. **More memory** — every object carries a header and references.",
"6. **Careful design needed up front** — a bad class hierarchy is expensive to fix later.",
"",
"---",
"",
"#### Program comparison",
"",
"*Procedural (C style):*",
"```c",
"struct Student { int roll; float marks; };",
"void display(struct Student s) { printf(\"%d %f\", s.roll, s.marks); }",
"/* Anyone can write s.marks = -50;  nothing prevents it */",
"```",
"",
"*Object oriented (Java):*",
"```java",
"class Student {",
"    private int roll;",
"    private float marks;          // hidden from outside",
"",
"    public void setMarks(float m) {",
"        if (m >= 0 && m <= 100) marks = m;   // validation is enforced here",
"    }",
"    public float getMarks() { return marks; }",
"    public void display() { System.out.println(roll + \" \" + marks); }",
"}",
"```",
"The invalid value can no longer get in — that is the practical meaning of encapsulation."
  ].join("\n");

  S["oop-concepts"] = [
"### The key elements of Object Oriented Programming",
"",
"The object model rests on **four major** and **three minor** elements.",
"",
"---",
"",
"#### 1. Object",
"",
"An **object** is a run-time entity that bundles **state**, **behaviour** and **identity**.",
"",
"- **State** — the values of its attributes at a given moment (a `Car` object's `speed = 60`).",
"- **Behaviour** — what it can do; the methods it responds to (`accelerate()`, `brake()`).",
"- **Identity** — what distinguishes it from every other object, even one with identical state. In Java, identity is the reference; `==` compares identity while `equals()` compares state.",
"",
"#### 2. Class",
"",
"A **class** is a blueprint or template that defines the attributes and methods common to a set of objects. The class is the *type*; objects are *instances* of it. A class occupies no memory; each object does.",
"",
"```java",
"class Rectangle {          // class = blueprint",
"    private double length, breadth;",
"    Rectangle(double l, double b) { length = l; breadth = b; }",
"    double area() { return length * breadth; }",
"}",
"Rectangle r1 = new Rectangle(5, 3);   // object = instance",
"```",
"",
"#### 3. Encapsulation",
"",
"**Wrapping data and the code that manipulates it into a single unit**, and hiding the data from the outside world. In Java it is achieved by declaring fields `private` and providing `public` getter/setter methods. It gives data security, controlled access with validation, and the freedom to change the internal representation later.",
"",
"#### 4. Abstraction",
"",
"**Showing only the essential features and hiding the implementation details.** A driver uses a steering wheel without knowing about the rack and pinion. Achieved in Java through **abstract classes** and **interfaces**. It reduces complexity and separates *what* an object does from *how* it does it.",
"",
"> **Encapsulation vs abstraction** — abstraction is a *design* concept concerned with hiding complexity (what to expose); encapsulation is an *implementation* concept concerned with hiding data (how to protect it). Abstraction solves the problem at design level; encapsulation at implementation level.",
"",
"#### 5. Inheritance",
"",
"A new class (**subclass / derived class**) acquires the properties and behaviour of an existing class (**superclass / base class**), and may add or modify them. It models the **\"is-a\"** relationship and is the principal mechanism for reuse.",
"",
"```java",
"class Vehicle { void start() { … } }",
"class Car extends Vehicle { void openBoot() { … } }   // Car is-a Vehicle",
"```",
"",
"#### 6. Polymorphism",
"",
"*\"Many forms\"* — the same interface behaves differently depending on the actual object.",
"",
"- **Compile-time (static)** — method **overloading**; resolved by the compiler from the argument list.",
"- **Run-time (dynamic)** — method **overriding**; resolved by the JVM from the actual object type.",
"",
"#### 7. Message passing",
"",
"Objects communicate by **sending messages** — that is, by invoking one another's methods. A message specifies the receiving object, the method name and the arguments.",
"",
"```java",
"account.deposit(5000);   //  receiver . message ( information )",
"```",
"",
"---",
"",
"#### Minor elements",
"",
"| Element | Meaning |",
"|---|---|",
"| **Typing** | Enforcement that objects of different types cannot be interchanged, or can only in restricted ways |",
"| **Concurrency** | Distinguishes an active object (with its own thread) from an inactive one |",
"| **Persistence** | The property by which an object's existence transcends time (saved to disk) and space |",
"",
"---",
"",
"#### The three models of the object model methodology",
"",
"| Model | Answers | Notation | Contains |",
"|---|---|---|---|",
"| **Object model** | *What* the system is made of — the static structure | Class diagram / object diagram | Classes, attributes, operations, associations, inheritance |",
"| **Dynamic model** | *When* things happen — control and sequencing | State diagram, sequence diagram | States, events, transitions, activities |",
"| **Functional model** | *How* data is transformed — what the system computes | Data-flow diagram | Processes, data flows, data stores, actors |",
"",
"The **object model** is the most important, because the other two are expressed in terms of the objects it defines.",
"",
"**Steps in constructing an object model:**",
"1. Identify candidate **objects and classes** from the problem statement (nouns).",
"2. Remove spurious and redundant classes.",
"3. Identify **associations** between classes (verbs) and remove the spurious ones.",
"4. Identify **attributes** of objects and links.",
"5. Organise classes using **inheritance** — generalise common attributes upward.",
"6. Group related classes into **modules/packages**.",
"7. Verify, iterate and refine the model."
  ].join("\n");

  S["java-basics"] = [
"### JVM, JRE, JDK and platform independence",
"",
"#### How Java runs",
"",
"```",
"   Hello.java  ──javac──▶  Hello.class      ──java──▶   output",
"  (source code)          (bytecode — the       (JVM interprets /",
"                          platform-neutral      JIT-compiles it for",
"                          intermediate form)    this machine)",
"```",
"",
"#### JVM — Java Virtual Machine",
"",
"An **abstract machine** — a specification, and its implementation, that provides the run-time environment in which Java bytecode is executed. The JVM is **platform dependent** (a different JVM exists for Windows, Linux, macOS), but the *bytecode it accepts is identical everywhere*. That is precisely what makes Java portable.",
"",
"**JVM sub-systems:**",
"1. **Class loader** — loads, links (verify, prepare, resolve) and initialises `.class` files.",
"2. **Runtime data areas** — Method area (class metadata), Heap (objects), Stack (per-thread frames), PC register, Native method stack.",
"3. **Execution engine** — Interpreter, **JIT compiler** (compiles hot bytecode into native code for speed) and **Garbage collector**.",
"",
"#### JRE — Java Runtime Environment",
"",
"$$\\text{JRE} = \\text{JVM} + \\text{core class libraries} + \\text{supporting files}$$",
"",
"It is what a *user* needs in order to **run** a Java application. It cannot compile.",
"",
"#### JDK — Java Development Kit",
"",
"$$\\text{JDK} = \\text{JRE} + \\text{development tools}$$",
"",
"It is what a *developer* needs in order to **write** Java applications. Tools included: `javac` (compiler), `java` (launcher), `javadoc` (documentation), `jar` (archiver), `jdb` (debugger), `javap` (disassembler).",
"",
"```",
"┌──────────────────── JDK ──────────────────────┐",
"│  javac  javadoc  jar  jdb  javap  …           │",
"│  ┌─────────────── JRE ────────────────────┐   │",
"│  │  Java class libraries (rt.jar / modules)│   │",
"│  │  ┌────────── JVM ──────────┐            │   │",
"│  │  │ Class loader            │            │   │",
"│  │  │ Runtime data areas      │            │   │",
"│  │  │ Execution engine (JIT)  │            │   │",
"│  │  └─────────────────────────┘            │   │",
"│  └────────────────────────────────────────┘   │",
"└───────────────────────────────────────────────┘",
"```",
"",
"| | JDK | JRE | JVM |",
"|---|---|---|---|",
"| Purpose | Develop + run | Run only | Execute bytecode |",
"| Contains | JRE + tools | JVM + libraries | Specification + implementation |",
"| Needed by | Developer | End user | Both (inside JRE) |",
"| Platform dependent | Yes | Yes | Yes |",
"",
"---",
"",
"#### Why Java is platform independent",
"",
"A C compiler produces machine code for one specific processor and operating system; that `.exe` will not run anywhere else. A Java compiler produces **bytecode**, an instruction set for the *virtual* machine rather than for any real one. Because a JVM exists for every platform and every JVM accepts the same bytecode, the **same `.class` file runs unchanged everywhere**. This is the meaning of the slogan **\"Write Once, Run Anywhere\" (WORA)**.",
"",
"Strictly: *Java is platform independent; the JVM is platform dependent.*",
"",
"---",
"",
"#### Primitive data types in Java",
"",
"| Type | Size | Range | Default |",
"|---|---|---|---|",
"| `byte` | 8 bits | $-128$ to $127$ | 0 |",
"| `short` | 16 bits | $-32{,}768$ to $32{,}767$ | 0 |",
"| `int` | 32 bits | $-2^{31}$ to $2^{31}-1$ | 0 |",
"| `long` | 64 bits | $-2^{63}$ to $2^{63}-1$ | 0L |",
"| `float` | 32 bits | ±3.4e38, 7 digits precision | 0.0f |",
"| `double` | 64 bits | ±1.7e308, 15 digits precision | 0.0d |",
"| `char` | 16 bits | Unicode 0 to 65,535 | '\\u0000' |",
"| `boolean` | JVM-dependent | `true` / `false` | false |",
"",
"Everything else in Java (`String`, arrays, objects) is a **reference type**. Note that the sizes are **fixed by the specification**, not by the machine — another contributor to portability."
  ].join("\n");

  S["encapsulation-abstraction"] = [
"### Encapsulation and data abstraction",
"",
"#### Encapsulation",
"",
"**Definition.** Encapsulation is the mechanism that **binds together the data and the methods that operate on that data**, and keeps both safe from outside interference and misuse. It is often called **data hiding**.",
"",
"**How it is achieved in Java:**",
"1. Declare all instance variables `private`.",
"2. Provide `public` **getter** and **setter** methods to read and modify them.",
"3. Put validation logic inside the setters.",
"",
"```java",
"public class BankAccount {",
"    private String holder;      // hidden",
"    private double balance;     // hidden",
"",
"    public BankAccount(String holder, double opening) {",
"        this.holder  = holder;",
"        this.balance = opening;",
"    }",
"",
"    public double getBalance() { return balance; }          // getter",
"",
"    public void deposit(double amount) {                    // controlled setter",
"        if (amount <= 0)",
"            throw new IllegalArgumentException(\"Deposit must be positive\");",
"        balance += amount;",
"    }",
"",
"    public void withdraw(double amount) {",
"        if (amount > balance)",
"            System.out.println(\"Insufficient funds\");",
"        else",
"            balance -= amount;",
"    }",
"}",
"",
"public class Demo {",
"    public static void main(String[] a) {",
"        BankAccount acc = new BankAccount(\"Yash\", 5000);",
"        // acc.balance = 1000000;   ← compile error: balance has private access",
"        acc.deposit(2000);",
"        acc.withdraw(10000);        // prints \"Insufficient funds\"",
"        System.out.println(acc.getBalance());   // 7000.0",
"    }",
"}",
"```",
"",
"#### The role of getters and setters",
"",
"| Purpose | Explanation |",
"|---|---|",
"| **Controlled access** | The class decides who may read and who may write |",
"| **Validation** | A setter can reject invalid values before they corrupt the object |",
"| **Read-only / write-only fields** | Supply only a getter, or only a setter |",
"| **Freedom to change the implementation** | The internal field can be renamed or its type changed without breaking any caller |",
"| **Debugging and logging** | A single place to trace every change to a field |",
"",
"---",
"",
"#### Data abstraction",
"",
"**Definition.** Abstraction is the act of **representing essential features without including the background details**. It answers *\"what does this object do?\"* and deliberately suppresses *\"how does it do it?\"*",
"",
"**How it is achieved in Java:**",
"- **Abstract classes** (0–100 % abstraction)",
"- **Interfaces** (100 % abstraction before Java 8)",
"",
"```java",
"abstract class Shape {",
"    abstract double area();                  // WHAT, not HOW",
"    void describe() {                        // concrete method allowed",
"        System.out.println(\"Area = \" + area());",
"    }",
"}",
"",
"class Circle extends Shape {",
"    private double r;",
"    Circle(double r) { this.r = r; }",
"    double area() { return Math.PI * r * r; }",
"}",
"",
"class Square extends Shape {",
"    private double s;",
"    Square(double s) { this.s = s; }",
"    double area() { return s * s; }",
"}",
"",
"// The user calls describe() without knowing either formula.",
"```",
"",
"---",
"",
"#### Comparison",
"",
"| Point | **Abstraction** | **Encapsulation** |",
"|---|---|---|",
"| Hides | Implementation / complexity | Data |",
"| Question answered | *What* an object does | *How* the data is protected |",
"| Level | Design level | Implementation level |",
"| Achieved by | Abstract classes, interfaces | Access modifiers (private) + getters/setters |",
"| Focus | Outside view of the object | Inside view of the object |",
"| Purpose | Reduce complexity for the user | Secure data from unauthorised access |",
"| Example | A `List` interface — you don't know if it is an array or a linked list | `private double balance` reachable only via `deposit()` |",
"",
"**Relationship.** They are complementary. Abstraction decides *what* to expose; encapsulation *enforces* that decision. A well-designed class uses abstraction to define a clean interface and encapsulation to guarantee that nothing outside can bypass it."
  ].join("\n");

  S["classes-objects"] = [
"### Class, method and object",
"",
"#### Class",
"",
"A **class** is a user-defined blueprint from which objects are created. It groups together **fields** (state) and **methods** (behaviour). Declaring a class does not allocate memory.",
"",
"```java",
"class ClassName {",
"    // fields  (instance variables)",
"    dataType fieldName;",
"",
"    // constructor",
"    ClassName(parameters) { … }",
"",
"    // methods",
"    returnType methodName(parameters) { … }",
"}",
"```",
"",
"#### Object",
"",
"An **object** is an instance of a class — a real entity in memory with its own copy of the instance variables.",
"",
"```java",
"ClassName ref = new ClassName(arguments);",
"//   ▲          ▲       ▲",
"//   │          │       └── constructor call, initialises the object",
"//   │          └────────── allocates memory on the heap",
"//   └───────────────────── reference variable on the stack",
"```",
"",
"#### Method",
"",
"A **method** is a named block of code that performs a task and optionally returns a value. Syntax:",
"$$\\text{accessModifier} \\;\\; [\\text{static}] \\;\\; \\text{returnType} \\;\\; \\text{name}(\\text{parameterList}) \\; \\{ \\; body \\; \\}$$",
"",
"---",
"",
"#### Complete worked example",
"",
"```java",
"public class Student {",
"",
"    // ---------- fields ----------",
"    private String name;",
"    private int    roll;",
"    private double marks;",
"    private static int count = 0;        // shared by all objects",
"",
"    // ---------- constructor ----------",
"    public Student(String name, int roll, double marks) {",
"        this.name  = name;               // 'this' distinguishes field from parameter",
"        this.roll  = roll;",
"        this.marks = marks;",
"        count++;",
"    }",
"",
"    // ---------- methods ----------",
"    public String getGrade() {",
"        if (marks >= 75) return \"Distinction\";",
"        else if (marks >= 60) return \"First\";",
"        else if (marks >= 45) return \"Second\";",
"        else return \"Fail\";",
"    }",
"",
"    public void display() {",
"        System.out.println(roll + \"  \" + name + \"  \" + marks + \"  \" + getGrade());",
"    }",
"",
"    public static int getCount() { return count; }   // static method",
"",
"    // ---------- main ----------",
"    public static void main(String[] args) {",
"        Student s1 = new Student(\"Yash\", 51, 82.5);",
"        Student s2 = new Student(\"Anita\", 52, 64.0);",
"",
"        s1.display();                    // 51  Yash  82.5  Distinction",
"        s2.display();                    // 52  Anita 64.0  First",
"        System.out.println(\"Total students: \" + Student.getCount());   // 2",
"    }",
"}",
"```",
"",
"#### What happens in memory",
"",
"```",
"        STACK                       HEAP",
"  ┌─────────────┐          ┌──────────────────────┐",
"  │ s1  ────────┼─────────▶│ name = \"Yash\"        │",
"  ├─────────────┤          │ roll = 51            │",
"  │ s2  ────────┼───┐      │ marks = 82.5         │",
"  └─────────────┘   │      └──────────────────────┘",
"                    │      ┌──────────────────────┐",
"                    └─────▶│ name = \"Anita\"       │",
"                           │ roll = 52            │",
"                           │ marks = 64.0         │",
"                           └──────────────────────┘",
"        METHOD AREA:  count = 2   (one copy, shared)",
"```",
"",
"---",
"",
"#### Varieties of classes",
"",
"| Kind | Description |",
"|---|---|",
"| **Concrete class** | Fully implemented; can be instantiated |",
"| **Abstract class** | Has at least one abstract method; cannot be instantiated |",
"| **Final class** | Cannot be extended (e.g. `String`) |",
"| **Static nested class** | A class inside another, not tied to an outer instance |",
"| **Inner class** | A non-static nested class, tied to an outer object |",
"| **Local class** | Declared inside a method |",
"| **Anonymous class** | Declared and instantiated in one expression |",
"| **Singleton class** | Designed so that only one instance can ever exist |",
"| **Wrapper class** | Wraps a primitive: `Integer`, `Double`, `Character` |",
"| **POJO / Bean** | Plain data-holding class with private fields and getters/setters |",
"",
"#### Object interaction",
"",
"Objects interact by **message passing** — one object invokes a method of another and may pass data (arguments). Interaction takes three common forms:",
"- **Association** — an object holds a reference to another and calls its methods.",
"- **Composition/Aggregation** — an object contains another as a part.",
"- **Inheritance** — a subclass object invokes inherited superclass behaviour, possibly via `super`."
  ].join("\n");

  S["static-members"] = [
"### Static data members and static methods",
"",
"#### The `static` keyword",
"",
"`static` makes a member belong to the **class itself** rather than to any individual object. A static member exists from the moment the class is loaded, before any object is created, and there is **exactly one copy** of it no matter how many objects are made.",
"",
"---",
"",
"#### Static variables (class variables)",
"",
"**Characteristics:**",
"- One copy shared by **all** objects of the class.",
"- Stored in the **method area**, not on the heap with the objects.",
"- Initialised once, when the class is loaded.",
"- Accessed by `ClassName.variable` (preferred) or through any object reference.",
"- Get default values automatically (0, 0.0, false, null).",
"",
"**Uses:** counters, constants (`static final`), configuration shared by all instances.",
"",
"```java",
"class Employee {",
"    private String name;",
"    private static String company = \"TCS\";      // shared by everyone",
"    private static int    empCount = 0;         // shared counter",
"    private static final double PF_RATE = 0.12; // shared constant",
"",
"    Employee(String name) {",
"        this.name = name;",
"        empCount++;                              // one shared counter increments",
"    }",
"",
"    void show() { System.out.println(name + \" @ \" + company); }",
"",
"    static int getCount() { return empCount; }",
"}",
"",
"public class Test {",
"    public static void main(String[] a) {",
"        new Employee(\"Yash\").show();     // Yash @ TCS",
"        new Employee(\"Riya\").show();     // Riya @ TCS",
"        System.out.println(Employee.getCount());   // 2  — no object needed",
"    }",
"}",
"```",
"",
"#### Static methods (class methods)",
"",
"**Characteristics:**",
"- Called on the class: `ClassName.method()` — no object required.",
"- Can access **only static** data directly.",
"- **Cannot** use `this` or `super`, because there is no current object.",
"- **Cannot be overridden** (they are hidden, not overridden — resolved at compile time by the reference type).",
"- Can be overloaded.",
"",
"**Why `main` is static:** the JVM must call `main` *before* any object of the class exists, so it cannot require an instance.",
"",
"```java",
"public static void main(String[] args)",
"```",
"",
"#### Static block",
"",
"Executed **once**, when the class is loaded, before `main` and before any object is created. Used for one-time initialisation of static data.",
"",
"```java",
"class Config {",
"    static int[] table;",
"    static {",
"        table = new int[10];",
"        for (int i = 0; i < 10; i++) table[i] = i * i;",
"        System.out.println(\"Static block ran once\");",
"    }",
"}",
"```",
"",
"#### Order of execution",
"```",
"static variables → static block → instance variables → instance block → constructor",
"```",
"",
"---",
"",
"#### Static vs instance members",
"",
"| Point | **Static member** | **Instance member** |",
"|---|---|---|",
"| Belongs to | The class | An object |",
"| Number of copies | One, shared | One per object |",
"| Memory area | Method area | Heap (inside the object) |",
"| Created when | Class is loaded | Object is created with `new` |",
"| Accessed by | `ClassName.member` | `objectRef.member` |",
"| Can use `this` | No | Yes |",
"| Can access instance data | Not directly | Yes |",
"| Overriding | Not possible (hiding only) | Possible |",
"| Example | `Math.PI`, `Math.sqrt()` | `student.getMarks()` |"
  ].join("\n");

  S["access-modifiers"] = [
"### Access modifiers (access specifiers)",
"",
"Access modifiers control the **visibility** of classes, fields, methods and constructors. Java has four levels.",
"",
"| Modifier | Same class | Same package | Subclass (other package) | Anywhere |",
"|---|---|---|---|---|",
"| `private` | ✔ | ✘ | ✘ | ✘ |",
"| *default* (no keyword) | ✔ | ✔ | ✘ | ✘ |",
"| `protected` | ✔ | ✔ | ✔ | ✘ |",
"| `public` | ✔ | ✔ | ✔ | ✔ |",
"",
"---",
"",
"#### 1. `private`",
"The most restrictive. Accessible **only inside the class in which it is declared** — not even to a subclass. Cannot be applied to a top-level class.",
"",
"*Use:* instance variables, helper methods, and constructors of singleton classes.",
"",
"```java",
"class Account {",
"    private double balance;              // nobody outside can touch it",
"    private void audit() { … }           // internal helper",
"    public double getBalance() { return balance; }",
"}",
"```",
"",
"#### 2. default (package-private)",
"No keyword is written. Accessible **within the same package only**. A subclass in a *different* package cannot see it.",
"",
"*Use:* classes and helpers meant to co-operate inside one package but not be part of the public API.",
"",
"#### 3. `protected`",
"Accessible within the package **and** to subclasses in any other package (through inheritance, on a reference of the subclass type).",
"",
"*Use:* members that a subclass legitimately needs, but that are not part of the public interface — the classic case is a method meant to be overridden.",
"",
"```java",
"package base;",
"public class Vehicle {",
"    protected int speed;                 // subclasses may use it",
"    protected void tune() { … }",
"}",
"",
"package derived;",
"import base.Vehicle;",
"public class Car extends Vehicle {",
"    void race() {",
"        speed = 200;                     // OK — inherited protected member",
"        tune();                          // OK",
"    }",
"}",
"```",
"",
"#### 4. `public`",
"Accessible from **everywhere**. A public top-level class must be declared in a file of the same name.",
"",
"*Use:* the class's API — the methods callers are meant to use, and `main`.",
"",
"---",
"",
"#### Access modifiers in inheritance",
"",
"When a subclass overrides a method, it **may widen but never narrow** the access:",
"",
"```java",
"class A { protected void show() { } }",
"class B extends A {",
"    public void show() { }        // OK — wider",
"    // private void show() { }    // ERROR — attempting to assign weaker access",
"}",
"```",
"",
"Similarly:",
"- A `private` member of the superclass is **inherited but not accessible** in the subclass.",
"- Constructors may be private (singleton), default, protected or public.",
"",
"#### Non-access modifiers (for completeness)",
"",
"| Modifier | Effect |",
"|---|---|",
"| `static` | Belongs to the class, not to objects |",
"| `final` | Variable = constant; method = cannot be overridden; class = cannot be extended |",
"| `abstract` | Method has no body; class cannot be instantiated |",
"| `synchronized` | Only one thread may execute the method at a time |",
"| `transient` | Field is skipped during serialisation |",
"| `volatile` | Field is always read from main memory, not a thread cache |",
"",
"#### Rule of thumb",
"",
"> Make every field `private`. Make a method `public` only if it is genuinely part of what the class offers to the outside world. This is encapsulation in practice."
  ].join("\n");

  S["constructors"] = [
"### Constructors",
"",
"#### Definition",
"",
"A **constructor** is a special method that is called automatically when an object is created with `new`, and whose job is to **initialise** the object.",
"",
"**Rules:**",
"1. Its name is **exactly the class name**.",
"2. It has **no return type**, not even `void`.",
"3. It is invoked implicitly by `new`; it cannot be called like an ordinary method.",
"4. It can be overloaded.",
"5. It can have any access modifier.",
"6. It cannot be `static`, `final`, `abstract` or `synchronized`.",
"7. If no constructor is written, the compiler supplies a **default constructor** with an empty body.",
"",
"---",
"",
"#### Types of constructors",
"",
"**1. Default constructor** — inserted by the compiler when the class defines no constructor at all. It takes no arguments and sets fields to their default values (0, 0.0, false, null).",
"",
"```java",
"class Box { int l, b; }",
"Box x = new Box();      // compiler-generated default constructor; l = b = 0",
"```",
"",
"> If you write *any* constructor, the compiler no longer supplies the default one.",
"",
"**2. No-argument constructor** — written explicitly with an empty parameter list, giving your own initial values.",
"",
"```java",
"class Box {",
"    int l, b;",
"    Box() { l = 1; b = 1; }         // no-arg constructor written by the programmer",
"}",
"```",
"",
"**3. Parameterised constructor** — takes arguments so each object can start with different values.",
"",
"```java",
"class Box {",
"    int l, b;",
"    Box(int l, int b) { this.l = l; this.b = b; }",
"}",
"Box x = new Box(10, 5);",
"```",
"",
"**4. Copy constructor** — creates a new object as a copy of an existing one. Java has no built-in copy constructor (unlike C++); you write it yourself.",
"",
"```java",
"class Box {",
"    int l, b;",
"    Box(int l, int b) { this.l = l; this.b = b; }",
"    Box(Box other) { this.l = other.l; this.b = other.b; }   // copy constructor",
"}",
"```",
"",
"---",
"",
"#### Constructor overloading and constructor chaining",
"",
"**Constructor chaining** is the practice of one constructor calling another, so that initialisation code is written once.",
"",
"- `this(...)` calls **another constructor of the same class**.",
"- `super(...)` calls a **constructor of the superclass**.",
"",
"Either must be the **very first statement** in the constructor — hence you cannot use both in one constructor.",
"",
"```java",
"class Box {",
"    int l, b, h;",
"",
"    Box()                    { this(1, 1, 1); }          // → 3-arg version",
"    Box(int side)            { this(side, side, side); } // → 3-arg version",
"    Box(int l, int b, int h) {                           // the real work happens here",
"        this.l = l; this.b = b; this.h = h;",
"        System.out.println(\"Box \" + l + \"x\" + b + \"x\" + h);",
"    }",
"}",
"",
"new Box();        // Box 1x1x1",
"new Box(5);       // Box 5x5x5",
"new Box(2,3,4);   // Box 2x3x4",
"```",
"",
"**Chaining up the inheritance hierarchy:**",
"",
"```java",
"class Animal {",
"    Animal(String s) { System.out.println(\"Animal: \" + s); }",
"}",
"class Dog extends Animal {",
"    Dog() {",
"        super(\"four legs\");            // must be first statement",
"        System.out.println(\"Dog created\");",
"    }",
"}",
"// new Dog()  prints:  Animal: four legs   /   Dog created",
"```",
"",
"If you do not write `super(...)`, the compiler inserts a call to the superclass's **no-argument** constructor. If the superclass has none, you get a compile error — which is why `super(...)` is often mandatory.",
"",
"---",
"",
"#### `this()` vs `super()`",
"",
"| | `this()` | `super()` |",
"|---|---|---|",
"| Calls | Another constructor of the **same** class | A constructor of the **parent** class |",
"| Purpose | Avoid duplicating initialisation code | Initialise the inherited part of the object |",
"| Position | Must be the first statement | Must be the first statement |",
"| Both in one constructor | Not allowed | Not allowed |",
"| Inserted automatically | No | Yes — `super()` if nothing else is written |",
"| Also exists as | `this.field`, `this.method()` | `super.field`, `super.method()` |",
"",
"---",
"",
"#### Destructors in Java",
"",
"Java has **no destructor**. Memory is reclaimed automatically by the **garbage collector** when an object becomes unreachable.",
"",
"- The old `finalize()` method was called by the GC before reclaiming an object, but its timing was never guaranteed; it is **deprecated** since Java 9.",
"- The correct modern practice for releasing non-memory resources (files, sockets, database connections) is `AutoCloseable` with **try-with-resources**:",
"",
"```java",
"try (BufferedReader br = new BufferedReader(new FileReader(\"a.txt\"))) {",
"    System.out.println(br.readLine());",
"}   // close() is called automatically, even if an exception is thrown",
"```",
"",
"| | Constructor | Destructor (C++) / GC (Java) |",
"|---|---|---|",
"| When | Object creation | Object destruction |",
"| Called | Explicitly via `new` | Automatically by the GC |",
"| Overloading | Yes | No |",
"| In Java | Yes | No destructor; garbage collector instead |"
  ].join("\n");

  S["message-passing"] = [
"### Message passing and argument passing",
"",
"#### Message passing",
"",
"In object oriented programming, objects do not read one another's data directly — they **communicate by sending messages**, that is, by invoking each other's methods.",
"",
"A message has three parts:",
"1. The **receiver** — the object being asked to do something",
"2. The **method name** — what it is being asked to do",
"3. The **arguments** — the information it needs",
"",
"```java",
"account.deposit(5000);",
"//  ▲       ▲       ▲",
"//  │       │       └── information (arguments)",
"//  │       └────────── the message / method name",
"//  └────────────────── the receiving object",
"```",
"",
"**Why it matters:** message passing keeps objects **loosely coupled**. The sender knows only *what* it is asking for, not *how* the receiver will do it — which is exactly what allows polymorphism and makes systems easy to extend.",
"",
"```java",
"class Engine {",
"    void start() { System.out.println(\"Engine started\"); }",
"}",
"class Car {",
"    private Engine engine = new Engine();",
"    void drive() {",
"        engine.start();          // Car sends the message start() to Engine",
"        System.out.println(\"Car moving\");",
"    }",
"}",
"```",
"",
"---",
"",
"#### Argument passing",
"",
"**Java is strictly pass-by-value.** What is copied depends on the type of the parameter.",
"",
"**1. Passing primitives — the value is copied**",
"",
"```java",
"class Test {",
"    static void change(int x) { x = 100; }",
"    public static void main(String[] a) {",
"        int n = 10;",
"        change(n);",
"        System.out.println(n);      // 10  — the original is unaffected",
"    }",
"}",
"```",
"",
"**2. Passing objects — the *reference* is copied**",
"",
"Both the caller's variable and the parameter now point to the **same object**, so changes to the object's fields *are* visible outside.",
"",
"```java",
"class Box { int size = 10; }",
"",
"class Test {",
"    static void modify(Box b)  { b.size = 99; }       // modifies the shared object",
"    static void reassign(Box b){ b = new Box(); b.size = 55; }  // rebinds the local copy only",
"",
"    public static void main(String[] a) {",
"        Box box = new Box();",
"        modify(box);",
"        System.out.println(box.size);   // 99  — the object was changed",
"",
"        reassign(box);",
"        System.out.println(box.size);   // 99  — still 99; the new Box was local",
"    }",
"}",
"```",
"",
"This is the crucial distinction: **the object can be modified, but the caller's reference can never be redirected.** That is why Java is *pass-by-value* (of the reference), not *pass-by-reference*.",
"",
"**3. Passing arrays** — arrays are objects, so the same rule applies; the array contents can be modified by the called method.",
"",
"---",
"",
"#### Summary",
"",
"| Passed | What is copied | Caller sees changes to the value? | Caller sees changes to the object's fields? |",
"|---|---|---|---|",
"| Primitive | The value | No | — |",
"| Object reference | The reference | No (reassignment is local) | **Yes** |",
"| Array | The reference | No | **Yes** |",
"",
"#### Variable-length arguments (varargs)",
"",
"```java",
"static int sum(int... nums) {          // treated as an int[] inside",
"    int t = 0;",
"    for (int n : nums) t += n;",
"    return t;",
"}",
"sum(1,2,3);        // 6",
"sum(1,2,3,4,5);    // 15",
"```",
"There can be only one varargs parameter, and it must be last."
  ].join("\n");

  S["inheritance"] = [
"### Inheritance",
"",
"#### Definition",
"",
"**Inheritance** is the mechanism by which one class acquires the fields and methods of another. The existing class is the **superclass** (base/parent); the new one is the **subclass** (derived/child). Java uses the keyword **`extends`**.",
"",
"It models the **\"is-a\"** relationship: a `Car` *is a* `Vehicle`.",
"",
"#### Purpose and uses",
"",
"1. **Code reusability** — write common behaviour once in the parent.",
"2. **Method overriding** — the basis of run-time polymorphism.",
"3. **Extensibility** — a new subclass can be added without touching existing code.",
"4. **Hierarchical classification** — models real taxonomies naturally.",
"5. **Maintainability** — a fix in the parent propagates to every child.",
"",
"---",
"",
"#### Types of inheritance",
"",
"**1. Single inheritance** — one subclass, one superclass.",
"",
"```",
"   A",
"   │",
"   B",
"```",
"```java",
"class Vehicle {",
"    protected String brand;",
"    void start() { System.out.println(\"Vehicle starting\"); }",
"}",
"",
"class Car extends Vehicle {",
"    int wheels = 4;",
"    void honk() { System.out.println(\"Beep!\"); }",
"}",
"",
"public class Demo {",
"    public static void main(String[] a) {",
"        Car c = new Car();",
"        c.start();      // inherited from Vehicle",
"        c.honk();       // its own",
"    }",
"}",
"```",
"",
"**2. Multilevel inheritance** — a chain.",
"",
"```",
"   A → B → C",
"```",
"```java",
"class Animal      { void eat()  { System.out.println(\"eating\"); } }",
"class Dog extends Animal { void bark() { System.out.println(\"barking\"); } }",
"class Puppy extends Dog  { void weep() { System.out.println(\"weeping\"); } }",
"// A Puppy object can call eat(), bark() and weep()",
"```",
"",
"**3. Hierarchical inheritance** — several subclasses from one superclass.",
"",
"```",
"      A",
"    ╱ │ ╲",
"   B  C  D",
"```",
"```java",
"class Shape { void draw() { } }",
"class Circle    extends Shape { }",
"class Rectangle extends Shape { }",
"class Triangle  extends Shape { }",
"```",
"",
"**4. Multiple inheritance** — one class with **two or more** superclasses.",
"",
"```",
"   A   B",
"    ╲ ╱",
"     C",
"```",
"**Java does not support multiple inheritance of classes**, to avoid the **diamond problem**: if both A and B define `show()`, C cannot decide which to inherit. Java achieves the same effect safely using **interfaces**, because an interface method has no body — there is nothing to conflict.",
"",
"```java",
"interface Printable { void print(); }",
"interface Showable  { void show(); }",
"",
"class Document implements Printable, Showable {   // 'multiple inheritance' in Java",
"    public void print() { System.out.println(\"printing\"); }",
"    public void show()  { System.out.println(\"showing\"); }",
"}",
"```",
"",
"**5. Hybrid inheritance** — a combination of the above. Achievable in Java only through interfaces.",
"",
"---",
"",
"#### Access specifiers in inheritance",
"",
"| Member in superclass | Visible in subclass (same package) | Visible in subclass (different package) |",
"|---|---|---|",
"| `private` | ✘ (inherited but inaccessible) | ✘ |",
"| default | ✔ | ✘ |",
"| `protected` | ✔ | ✔ |",
"| `public` | ✔ | ✔ |",
"",
"A `private` member **is** part of the subclass object in memory, but the subclass cannot name it — it must go through an inherited public/protected accessor.",
"",
"---",
"",
"#### The `super` keyword",
"",
"| Use | Meaning |",
"|---|---|",
"| `super.field` | The parent's field, when the child hides it |",
"| `super.method()` | The parent's version of an overridden method |",
"| `super(args)` | The parent's constructor — must be the first statement |",
"",
"```java",
"class Employee {",
"    protected String name;",
"    protected double salary;",
"    Employee(String n, double s) { name = n; salary = s; }",
"    void display() { System.out.println(name + \" earns \" + salary); }",
"}",
"",
"class Manager extends Employee {",
"    private double bonus;",
"    Manager(String n, double s, double b) {",
"        super(n, s);                       // initialise the inherited part",
"        bonus = b;",
"    }",
"    @Override",
"    void display() {",
"        super.display();                   // reuse the parent's work",
"        System.out.println(\"Bonus: \" + bonus);",
"    }",
"}",
"```",
"",
"---",
"",
"#### Points to remember",
"",
"- Constructors are **not inherited**, though they are invoked via `super()`.",
"- A `final` class cannot be extended (`String`, `Integer`).",
"- A `final` method cannot be overridden.",
"- Every class implicitly extends **`Object`** if nothing else is stated.",
"- Prefer **composition over inheritance** when the relationship is \"has-a\" rather than \"is-a\"."
  ].join("\n");

  S["association-aggregation"] = [
"### Association, aggregation and composition",
"",
"These are the three ways objects can be **related** without inheritance. All three model a **\"has-a\"** relationship, differing in the strength of the bond.",
"",
"---",
"",
"#### 1. Association",
"",
"The most general relationship: two objects are **aware of each other** and can interact, but neither owns the other. Both have independent lifetimes.",
"",
"```java",
"class Doctor {",
"    private String name;",
"    Doctor(String n) { name = n; }",
"    void treat(Patient p) { System.out.println(name + \" treats \" + p.getName()); }",
"}",
"class Patient {",
"    private String name;",
"    Patient(String n) { name = n; }",
"    String getName() { return name; }",
"}",
"// A doctor may treat many patients; a patient may see many doctors.",
"// Deleting one does not delete the other.",
"```",
"",
"**Multiplicity (cardinality) of association:**",
"",
"| Type | Meaning | Example |",
"|---|---|---|",
"| **One-to-one** | One object of A relates to exactly one of B | A `Person` has one `Passport` |",
"| **One-to-many** | One object of A relates to many of B | A `Department` has many `Teachers` |",
"| **Many-to-one** | Many objects of A relate to one of B | Many `Students` in one `College` |",
"| **Many-to-many** | Many of A relate to many of B | `Students` enrol in many `Courses` |",
"",
"**Class diagram notation:**",
"```",
"  ┌─────────┐  1        *  ┌─────────┐",
"  │ College ├──────────────┤ Student │      one-to-many",
"  └─────────┘              └─────────┘",
"",
"  ┌─────────┐  1        1  ┌──────────┐",
"  │ Person  ├──────────────┤ Passport │     one-to-one",
"  └─────────┘              └──────────┘",
"```",
"",
"---",
"",
"#### 2. Aggregation — a *weak* has-a",
"",
"A special association meaning **\"part of\"**, where the part can exist **independently** of the whole. Represented in UML by a **hollow diamond** at the whole.",
"",
"```java",
"class Address {",
"    String city, state;",
"    Address(String c, String s) { city = c; state = s; }",
"}",
"",
"class Student {",
"    private String name;",
"    private Address address;              // aggregation — Student HAS-A Address",
"",
"    Student(String name, Address address) {",
"        this.name = name;",
"        this.address = address;           // the Address was created outside",
"    }",
"    void display() { System.out.println(name + \", \" + address.city); }",
"}",
"",
"public class Demo {",
"    public static void main(String[] a) {",
"        Address addr = new Address(\"Jabalpur\", \"MP\");",
"        Student s1 = new Student(\"Yash\", addr);",
"        Student s2 = new Student(\"Riya\", addr);   // the SAME Address is shared",
"        s1.display();",
"        s2.display();",
"        // If s1 and s2 are destroyed, addr still exists.",
"    }",
"}",
"```",
"",
"Classic example: a `Department` aggregates `Teacher` objects — closing the department does not delete the teachers.",
"",
"---",
"",
"#### 3. Composition — a *strong* has-a",
"",
"The part **cannot exist without** the whole; the whole owns and controls the part's lifetime. UML: **filled diamond**.",
"",
"```java",
"class Engine {",
"    void start() { System.out.println(\"Engine on\"); }",
"}",
"",
"class Car {",
"    private final Engine engine = new Engine();   // created INSIDE — composition",
"    void drive() { engine.start(); System.out.println(\"Car moving\"); }",
"}",
"// Destroy the Car and the Engine goes with it. No one else can hold that Engine.",
"```",
"",
"Classic example: a `House` and its `Rooms`; a `Human` and its `Heart`.",
"",
"---",
"",
"#### Comparison",
"",
"| Point | **Association** | **Aggregation** | **Composition** |",
"|---|---|---|---|",
"| Relationship | \"uses-a\" | \"has-a\" (weak) | \"part-of\" (strong) |",
"| Ownership | None | Weak — part is shared | Strong — part is owned |",
"| Lifetime | Fully independent | Part outlives the whole | Part dies with the whole |",
"| UML symbol | Plain line | Hollow diamond ◇ | Filled diamond ◆ |",
"| Object created | Outside | Outside, passed in | Inside the owner |",
"| Sharing | Possible | Possible | Not possible |",
"| Example | Doctor – Patient | Department – Teacher | Car – Engine |",
"",
"> **Relationship between them:** Composition is a stronger form of Aggregation, and Aggregation is a special form of Association. Every composition is an aggregation, and every aggregation is an association — but not the reverse.",
"",
"---",
"",
"#### Types of aggregation",
"",
"| Type | Meaning |",
"|---|---|",
"| **Fixed aggregation** | The number and type of parts is fixed at design time (a `Bicycle` always has 2 `Wheel`s) |",
"| **Variable aggregation** | The number of parts varies at run time, but the levels are fixed (a `Company` has many `Employees`) |",
"| **Recursive aggregation** | An object contains instances of its own type, directly or indirectly (a `Directory` contains `Directory` objects; a program contains blocks that contain blocks) |"
  ].join("\n");

  S["abstract-interface"] = [
"### Abstract classes and interfaces",
"",
"#### Abstract class",
"",
"An **abstract class** is a class declared with the keyword `abstract` that **cannot be instantiated**. It exists to be extended, and to state a common contract plus some shared implementation for its subclasses.",
"",
"**Rules:**",
"1. Declared with `abstract class`.",
"2. May contain **abstract methods** (declared without a body) and **concrete methods** (with a body).",
"3. If a class has even one abstract method, the class **must** be abstract.",
"4. A subclass must implement **all** inherited abstract methods, or itself be declared abstract.",
"5. It **can** have constructors, instance variables, static methods and any access modifier.",
"6. It cannot be `final` (that would make it useless).",
"",
"---",
"",
"#### Can we create an object of an abstract class?",
"",
"**No — and here is why.** An abstract class is *incomplete*: it declares methods with no implementation. If an object could be created, calling such a method would have no code to execute. The compiler therefore rejects it.",
"",
"```java",
"abstract class Shape {",
"    abstract double area();                 // no body — incomplete",
"    void show() { System.out.println(\"Area = \" + area()); }",
"}",
"",
"class Circle extends Shape {",
"    private double r;",
"    Circle(double r) { this.r = r; }",
"    double area() { return Math.PI * r * r; }",
"}",
"",
"public class Demo {",
"    public static void main(String[] a) {",
"        // Shape s = new Shape();     ← COMPILE ERROR: Shape is abstract",
"",
"        Shape s = new Circle(5);      // ✔ legal — reference of parent type,",
"        s.show();                     //   object of concrete subclass",
"    }",
"}",
"```",
"",
"**Important nuance:** although you cannot instantiate it, you **can** hold an abstract-class *reference* pointing to a subclass object — and that is exactly how run-time polymorphism is achieved. You can also create an **anonymous subclass** on the spot:",
"",
"```java",
"Shape s = new Shape() {           // anonymous concrete subclass, not the abstract class itself",
"    double area() { return 0; }",
"};",
"```",
"",
"An abstract class **does** have a constructor — it runs when a subclass object is created, via `super()`.",
"",
"---",
"",
"#### Interface",
"",
"An **interface** is a fully abstract type that specifies **what** a class must be able to do, without saying anything about how. It is a pure contract.",
"",
"```java",
"interface Drawable {",
"    int MAX = 100;                 // implicitly public static final",
"    void draw();                   // implicitly public abstract",
"}",
"",
"class Circle implements Drawable {",
"    public void draw() { System.out.println(\"Drawing circle\"); }",
"}",
"```",
"",
"**Rules:**",
"1. All methods are implicitly `public abstract` (before Java 8).",
"2. All fields are implicitly `public static final` — constants.",
"3. Cannot be instantiated; cannot have a constructor.",
"4. A class **implements** an interface; an interface **extends** another interface.",
"5. Since **Java 8**: `default` and `static` methods with bodies are allowed. Since **Java 9**: `private` methods too.",
"",
"#### Can an interface extend another interface?",
"",
"**Yes.** An interface may extend one or many interfaces using `extends` (not `implements`), and a class implementing the child must implement all methods from the whole chain.",
"",
"```java",
"interface Printable { void print(); }",
"interface Showable extends Printable { void show(); }      // interface extends interface",
"",
"interface Both extends Printable, Showable { }             // multiple — allowed for interfaces",
"",
"class Document implements Showable {",
"    public void print() { System.out.println(\"printing\"); }  // from Printable",
"    public void show()  { System.out.println(\"showing\"); }   // from Showable",
"}",
"```",
"",
"---",
"",
"#### Multiple inheritance through interfaces",
"",
"Java forbids a class from extending two classes (the diamond problem), but permits implementing any number of interfaces — because an interface method has **no body**, so there is nothing to conflict over.",
"",
"```java",
"interface Engine  { void start(); }",
"interface Music   { void play(); }",
"interface GPS     { void navigate(); }",
"",
"class SmartCar implements Engine, Music, GPS {",
"    public void start()    { System.out.println(\"Engine started\"); }",
"    public void play()     { System.out.println(\"Playing music\"); }",
"    public void navigate() { System.out.println(\"Navigating\"); }",
"}",
"```",
"",
"---",
"",
"#### Abstract class vs Interface",
"",
"| Point | **Abstract class** | **Interface** |",
"|---|---|---|",
"| Keyword | `abstract class` | `interface` |",
"| Inherited by | `extends` | `implements` |",
"| Methods | Abstract **and** concrete | Abstract (+ default/static since Java 8) |",
"| Variables | Any kind: final, non-final, static, instance | Only `public static final` constants |",
"| Access modifiers | Any | Members implicitly public |",
"| Constructor | Yes | No |",
"| Multiple inheritance | Not supported | **Supported** |",
"| State | Can hold state | Cannot hold instance state |",
"| When to use | Related classes sharing common code | Unrelated classes sharing a capability |",
"| Speed | Slightly faster | Slightly slower (extra indirection) |",
"| Example | `AbstractList` | `Comparable`, `Runnable` |",
"",
"**Rule of thumb:** use an abstract class for an **\"is-a\"** relationship where subclasses share code; use an interface for a **\"can-do\"** capability that unrelated classes may all possess.",
"",
"---",
"",
"#### Program — employee details using an interface",
"",
"```java",
"interface EmployeeDetails {",
"    void setDetails(String name, String dept, int age);",
"    void display();",
"}",
"",
"class Employee implements EmployeeDetails {",
"    private String name, dept;",
"    private int age;",
"",
"    public void setDetails(String name, String dept, int age) {",
"        this.name = name;  this.dept = dept;  this.age = age;",
"    }",
"",
"    public void display() {",
"        System.out.println(\"Name      : \" + name);",
"        System.out.println(\"Department: \" + dept);",
"        System.out.println(\"Age       : \" + age);",
"    }",
"}",
"",
"public class Main {",
"    public static void main(String[] args) {",
"        EmployeeDetails e = new Employee();      // interface reference",
"        e.setDetails(\"Yash Khan\", \"AI & DS\", 20);",
"        e.display();",
"    }",
"}",
"```",
"",
"**Output:**",
"```",
"Name      : Yash Khan",
"Department: AI & DS",
"Age       : 20",
"```"
  ].join("\n");

  S["polymorphism"] = [
"### Polymorphism",
"",
"#### Definition",
"",
"**Polymorphism** (Greek: *poly* = many, *morph* = form) is the ability of one interface to take **many forms** — the same method name behaving differently depending on the context or on the actual object.",
"",
"#### Importance in OOP",
"",
"1. **One interface, many implementations** — the caller writes one line of code that works for every subtype.",
"2. **Extensibility** — a new subclass can be added and used by existing code **without modifying that code** (the Open/Closed Principle).",
"3. **Reduced coupling** — code depends on an abstraction, not on concrete classes.",
"4. **Cleaner, shorter code** — no long `if/else` or `switch` chains on type.",
"5. **Enables frameworks and design patterns** — Strategy, Factory, Template Method all rest on polymorphism.",
"",
"---",
"",
"#### The two forms",
"",
"#### 1. Compile-time (static) polymorphism — method overloading",
"",
"Several methods share a name but differ in their **parameter list** (number, type or order). The compiler picks the right one from the arguments.",
"",
"```java",
"class Calculator {",
"    int    add(int a, int b)            { return a + b; }",
"    int    add(int a, int b, int c)     { return a + b + c; }   // different count",
"    double add(double a, double b)      { return a + b; }       // different type",
"    String add(String a, String b)      { return a + b; }",
"}",
"",
"Calculator c = new Calculator();",
"c.add(2, 3);            // → 5      (int version)",
"c.add(2, 3, 4);         // → 9      (three-arg version)",
"c.add(2.5, 3.5);        // → 6.0    (double version)",
"c.add(\"Ja\", \"va\");      // → Java   (String version)",
"```",
"",
"> The **return type alone cannot** distinguish overloaded methods — the compiler would have no way to choose.",
"",
"#### 2. Run-time (dynamic) polymorphism — method overriding",
"",
"A subclass provides its **own implementation** of a method already defined in the superclass, with the **same signature**. The JVM decides which version to run based on the **actual object**, not the reference type.",
"",
"```java",
"class Animal {",
"    void sound() { System.out.println(\"Some sound\"); }",
"}",
"class Dog extends Animal {",
"    @Override void sound() { System.out.println(\"Woof\"); }",
"}",
"class Cat extends Animal {",
"    @Override void sound() { System.out.println(\"Meow\"); }",
"}",
"",
"public class Demo {",
"    public static void main(String[] a) {",
"        Animal[] zoo = { new Dog(), new Cat(), new Animal() };",
"        for (Animal x : zoo) x.sound();   // Woof / Meow / Some sound",
"    }",
"}",
"```",
"",
"The reference type is `Animal` in every case, yet three different methods run — that is **dynamic method dispatch**.",
"",
"---",
"",
"#### How the JVM handles overriding at run time",
"",
"1. At **compile time**, the compiler checks only that the method exists in the *reference* type (`Animal.sound()`). If it does not, compilation fails.",
"2. Each class has a **method table (vtable)** built by the JVM, holding pointers to its actual method implementations. An overriding method **replaces** the parent's entry.",
"3. At **run time**, the JVM looks at the object's header, finds its class, looks up `sound()` in that class's method table, and invokes it. This is **late binding / dynamic dispatch**, implemented by the `invokevirtual` bytecode.",
"4. The JIT compiler often optimises this away when only one implementation is loaded (monomorphic inline caching).",
"",
"---",
"",
"#### Overloading vs Overriding",
"",
"| Point | **Overloading** | **Overriding** |",
"|---|---|---|",
"| Also called | Compile-time / static polymorphism | Run-time / dynamic polymorphism |",
"| Where | Within the **same class** (or a subclass) | Across **superclass and subclass** |",
"| Parameters | **Must differ** | **Must be identical** |",
"| Return type | May differ | Must be same or covariant |",
"| Binding | Early (compile time) | Late (run time) |",
"| Inheritance needed | No | **Yes** |",
"| `private`/`static`/`final` methods | Can be overloaded | **Cannot** be overridden |",
"| Access modifier | Free | Cannot be more restrictive than the parent's |",
"| Exceptions | Free | Cannot throw broader checked exceptions |",
"| Performance | Faster (resolved at compile time) | Slight run-time lookup cost |",
"| Purpose | Convenience — one name for related operations | Specialisation — redefine inherited behaviour |",
"",
"---",
"",
"#### Using both in one program",
"",
"Yes — they are independent mechanisms and commonly appear together.",
"",
"```java",
"class Shape {",
"    void draw()          { System.out.println(\"Drawing a shape\"); }",
"    void draw(String c)  { System.out.println(\"Drawing a shape in \" + c); }  // overload",
"}",
"",
"class Circle extends Shape {",
"    @Override void draw()         { System.out.println(\"Drawing a circle\"); }        // override",
"    @Override void draw(String c) { System.out.println(\"Drawing a circle in \" + c); }// override",
"    void draw(int radius)         { System.out.println(\"Circle of r=\" + radius); }   // overload",
"}",
"",
"public class Demo {",
"    public static void main(String[] a) {",
"        Shape s = new Circle();",
"        s.draw();               // Drawing a circle        ← overriding at run time",
"        s.draw(\"red\");          // Drawing a circle in red ← overriding, overloaded signature",
"        // s.draw(5);           ← compile error: Shape has no draw(int)",
"        ((Circle) s).draw(5);   // Circle of r=5",
"    }",
"}",
"```",
"",
"---",
"",
"#### Early binding vs late binding",
"",
"| | **Early (static) binding** | **Late (dynamic) binding** |",
"|---|---|---|",
"| Resolved at | Compile time | Run time |",
"| Uses | The **reference** type | The **object** type |",
"| Applies to | `static`, `private`, `final` methods; overloaded methods; all variables | Overridden instance methods |",
"| Speed | Faster | Slightly slower |",
"| Flexibility | Low | High |",
"",
"```java",
"class A { static void s() { System.out.println(\"A.s\"); }",
"          void i()        { System.out.println(\"A.i\"); } }",
"class B extends A { static void s() { System.out.println(\"B.s\"); }",
"                    void i()        { System.out.println(\"B.i\"); } }",
"",
"A ref = new B();",
"ref.s();     // A.s  ← early binding: chosen from the REFERENCE type",
"ref.i();     // B.i  ← late binding: chosen from the OBJECT type",
"```",
"",
"#### How polymorphism promotes extensibility",
"",
"```java",
"// Existing code — written once, never touched again:",
"void renderAll(Shape[] shapes) {",
"    for (Shape s : shapes) s.draw();",
"}",
"",
"// Six months later a new shape is added:",
"class Hexagon extends Shape { @Override void draw() { … } }",
"```",
"`renderAll` handles `Hexagon` correctly **without a single change**. Without polymorphism it would need a new `if` branch every time — and every such edit risks breaking working code."
  ].join("\n");

  S["multithreading"] = [
"### Multithreading in Java",
"",
"#### Process vs Thread",
"",
"| Point | **Process** | **Thread** |",
"|---|---|---|",
"| Definition | A program in execution | A lightweight sub-unit of a process |",
"| Memory | Its own separate address space | Shares the process's memory and files |",
"| Creation cost | Heavy — OS allocates new memory | Light — only a stack and PC |",
"| Context switch | Slow | Fast |",
"| Communication | IPC: pipes, sockets, shared memory | Direct — shared variables |",
"| Isolation | One crash does not kill others | One uncaught error can bring down the process |",
"| Also called | Heavyweight | Lightweight |",
"",
"**Multiprocessing** runs several *processes*; **multithreading** runs several *threads* inside one process. Multithreading is faster and uses less memory, but requires careful synchronisation because the threads share data.",
"",
"---",
"",
"#### Thread life cycle (thread states)",
"",
"```",
"                start()          scheduler picks it",
"   ┌─────┐  ────────────▶ ┌──────────┐ ─────────────▶ ┌─────────┐",
"   │ NEW │                │ RUNNABLE │◀───────────    │ RUNNING │",
"   └─────┘                └──────────┘   yield /      └────┬────┘",
"                               ▲       time slice ends     │",
"                               │                           │ sleep(), wait(),",
"                               │  notify(), timeout,       │ join(), blocking I/O",
"                               │  I/O complete             ▼",
"                               └─────────────────  ┌──────────────┐",
"                                                   │ BLOCKED /    │",
"                                                   │ WAITING /    │",
"                                                   │ TIMED_WAITING│",
"                                                   └──────────────┘",
"                    run() returns  ──▶ ┌────────────┐",
"                                       │ TERMINATED │",
"                                       └────────────┘",
"```",
"",
"| State | Meaning |",
"|---|---|",
"| **NEW** | Object created, `start()` not yet called |",
"| **RUNNABLE** | Ready to run, waiting for the CPU |",
"| **RUNNING** | Actually executing on a CPU |",
"| **BLOCKED** | Waiting to acquire a monitor lock |",
"| **WAITING** | Waiting indefinitely — `wait()`, `join()` |",
"| **TIMED_WAITING** | Waiting for a set time — `sleep(t)`, `wait(t)` |",
"| **TERMINATED** | `run()` has completed or the thread was stopped |",
"",
"---",
"",
"#### The two ways of creating a thread",
"",
"**Method 1 — extend `Thread`**",
"",
"```java",
"class MyThread extends Thread {",
"    private String name;",
"    MyThread(String name) { this.name = name; }",
"",
"    @Override",
"    public void run() {                 // the thread's body",
"        for (int i = 1; i <= 5; i++) {",
"            System.out.println(name + \" : \" + i);",
"            try { Thread.sleep(300); } catch (InterruptedException e) { }",
"        }",
"    }",
"}",
"",
"public class Demo1 {",
"    public static void main(String[] a) {",
"        MyThread t1 = new MyThread(\"Thread-A\");",
"        MyThread t2 = new MyThread(\"Thread-B\");",
"        t1.start();          // start() — NOT run()",
"        t2.start();",
"    }",
"}",
"```",
"",
"**Method 2 — implement `Runnable`**",
"",
"```java",
"class MyTask implements Runnable {",
"    private String name;",
"    MyTask(String name) { this.name = name; }",
"",
"    @Override",
"    public void run() {",
"        for (int i = 1; i <= 5; i++)",
"            System.out.println(name + \" : \" + i);",
"    }",
"}",
"",
"public class Demo2 {",
"    public static void main(String[] a) {",
"        Thread t1 = new Thread(new MyTask(\"Task-A\"));",
"        Thread t2 = new Thread(new MyTask(\"Task-B\"));",
"        t1.start();",
"        t2.start();",
"",
"        // Java 8 lambda, since Runnable is a functional interface:",
"        new Thread(() -> System.out.println(\"Lambda thread\")).start();",
"    }",
"}",
"```",
"",
"#### Which is preferred, and why",
"",
"**`Runnable` is preferred**, for four reasons:",
"",
"1. **Java allows only single inheritance.** Extending `Thread` uses up the one `extends` slot, so the class can never extend anything else. Implementing `Runnable` leaves it free.",
"2. **Better separation of concerns** — `Runnable` represents the *task*; `Thread` represents the *worker*. Mixing them is poor design.",
"3. **Reusability** — one `Runnable` instance can be handed to several threads, or submitted to an `ExecutorService` thread pool.",
"4. **Loose coupling** — the task does not inherit the many methods of `Thread` it does not need.",
"",
"> **Critical point:** always call **`start()`**, never `run()`. Calling `run()` directly simply executes the method on the *current* thread — no new thread is created at all.",
"",
"---",
"",
"#### Thread priorities",
"",
"Every thread has a priority from **1 to 10** that hints to the scheduler how much CPU time it should get.",
"",
"| Constant | Value |",
"|---|---|",
"| `Thread.MIN_PRIORITY` | 1 |",
"| `Thread.NORM_PRIORITY` | 5 (default) |",
"| `Thread.MAX_PRIORITY` | 10 |",
"",
"```java",
"Thread t1 = new Thread(task, \"Low\");",
"Thread t2 = new Thread(task, \"High\");",
"",
"t1.setPriority(Thread.MIN_PRIORITY);   // 1",
"t2.setPriority(Thread.MAX_PRIORITY);   // 10",
"System.out.println(t2.getPriority());  // 10",
"",
"t1.start();  t2.start();",
"```",
"",
"A thread inherits its creator's priority by default. `setPriority()` throws `IllegalArgumentException` outside 1–10.",
"",
"**Important caveat:** priorities are only a **hint**. The actual behaviour depends on the underlying OS scheduler, so a program must **never** rely on priority for correctness.",
"",
"---",
"",
"#### Synchronization",
"",
"**The problem — a race condition.** When two threads modify shared data at the same time, the interleaving of their operations can corrupt it.",
"",
"```java",
"class Counter {",
"    private int count = 0;",
"    public void increment() { count++; }   // NOT atomic: read, add, write",
"    public int get() { return count; }",
"}",
"// Two threads each incrementing 10,000 times often ends below 20,000.",
"```",
"",
"**The solution.** `synchronized` makes a method or block require the object's **monitor lock**; only one thread can hold it at a time.",
"",
"```java",
"class Counter {",
"    private int count = 0;",
"",
"    public synchronized void increment() { count++; }   // synchronized method",
"",
"    public void incrementBlock() {",
"        synchronized (this) {          // synchronized block — finer grained",
"            count++;",
"        }",
"    }",
"}",
"```",
"",
"**When to use it:** whenever two or more threads may access **mutable shared state** and at least one of them writes. If the data is immutable or thread-confined, no synchronization is needed.",
"",
"**Costs:** locking is slower, and careless locking can produce **deadlock** (two threads each holding the lock the other needs).",
"",
"---",
"",
"#### Inter-thread communication",
"",
"Three `final` methods of `Object`, callable only inside a synchronized context:",
"",
"| Method | Effect |",
"|---|---|",
"| `wait()` | Releases the lock and suspends the thread until notified |",
"| `notify()` | Wakes **one** waiting thread |",
"| `notifyAll()` | Wakes **all** waiting threads |",
"",
"**Producer–Consumer example:**",
"",
"```java",
"class Buffer {",
"    private int data;",
"    private boolean empty = true;",
"",
"    public synchronized void produce(int value) throws InterruptedException {",
"        while (!empty) wait();            // wait until the consumer takes it",
"        data = value;  empty = false;",
"        System.out.println(\"Produced \" + value);",
"        notifyAll();",
"    }",
"",
"    public synchronized int consume() throws InterruptedException {",
"        while (empty) wait();             // wait until the producer puts something",
"        empty = true;",
"        System.out.println(\"Consumed \" + data);",
"        notifyAll();",
"        return data;",
"    }",
"}",
"```",
"",
"> Always call `wait()` inside a **`while`** loop, never an `if` — a thread can wake spuriously.",
"",
"---",
"",
"#### Important Thread class methods",
"",
"| Method | Purpose |",
"|---|---|",
"| `start()` | Creates a new thread and calls `run()` on it |",
"| `run()` | The body of the thread |",
"| `sleep(ms)` | Pauses the current thread; **keeps** any locks held |",
"| `join()` | Waits for another thread to finish |",
"| `interrupt()` | Sets the interrupt flag; a sleeping/waiting thread throws `InterruptedException` |",
"| `isAlive()` | Has the thread started and not yet terminated? |",
"| `yield()` | Hints that the thread is willing to give up the CPU |",
"| `setDaemon(true)` | Marks a background thread that does not prevent JVM exit |",
"| `setName()` / `getName()` | Thread identification |",
"",
"#### `sleep()` vs `wait()`",
"",
"| | `sleep()` | `wait()` |",
"|---|---|---|",
"| Class | `Thread` | `Object` |",
"| Releases the lock | **No** | **Yes** |",
"| Wakes up when | Time expires | `notify()` / `notifyAll()` / timeout |",
"| Must be in synchronized block | No | **Yes** |"
  ].join("\n");

  S["exception-handling"] = [
"### Exception handling in Java",
"",
"#### What an exception is",
"",
"An **exception** is an event that disrupts the normal flow of a program at run time. Java represents it as an **object** thrown at the point of the error and caught wherever the programmer chooses to deal with it.",
"",
"#### The exception hierarchy",
"",
"```",
"                       Throwable",
"                      ╱         ╲",
"                 Error           Exception",
"          (unrecoverable)       ╱          ╲",
"      StackOverflowError   RuntimeException   IOException",
"      OutOfMemoryError    (unchecked)         SQLException",
"                            │                 FileNotFoundException",
"                            ├─ ArithmeticException      (CHECKED)",
"                            ├─ NullPointerException",
"                            ├─ ArrayIndexOutOfBoundsException",
"                            ├─ ClassCastException",
"                            └─ NumberFormatException",
"```",
"",
"---",
"",
"#### Checked vs unchecked exceptions",
"",
"| Point | **Checked** | **Unchecked** |",
"|---|---|---|",
"| Also called | Compile-time exceptions | Run-time exceptions |",
"| Checked by compiler | **Yes** — must be handled or declared | No |",
"| Superclass | `Exception` (not `RuntimeException`) | `RuntimeException` / `Error` |",
"| Cause | External conditions beyond the program's control | Programming mistakes |",
"| If unhandled | **Compile error** | Compiles; crashes at run time |",
"| Examples | `IOException`, `SQLException`, `FileNotFoundException`, `ClassNotFoundException`, `InterruptedException` | `ArithmeticException`, `NullPointerException`, `ArrayIndexOutOfBoundsException`, `NumberFormatException` |",
"| Recovery | Usually possible and expected | Should be prevented, not caught |",
"",
"---",
"",
"#### The five keywords",
"",
"| Keyword | Purpose |",
"|---|---|",
"| `try` | Encloses the code that may throw an exception |",
"| `catch` | Handles a particular type of exception |",
"| `finally` | Always executes — used for cleanup |",
"| `throw` | **Explicitly throws** one exception object |",
"| `throws` | **Declares** that a method may propagate exceptions to its caller |",
"",
"#### `throw` vs `throws`",
"",
"| | `throw` | `throws` |",
"|---|---|---|",
"| Used to | Actually throw an exception | Declare exceptions in a method signature |",
"| Placed | Inside the method body | After the parameter list |",
"| Followed by | An **instance** | One or more **class names** |",
"| Number | One at a time | Several, comma-separated |",
"| Example | `throw new IOException(\"failed\");` | `void read() throws IOException, SQLException` |",
"",
"---",
"",
"#### Complete worked program",
"",
"```java",
"import java.util.Scanner;",
"",
"public class ExceptionDemo {",
"",
"    // 'throws' declares that this method may propagate the exception",
"    static void checkAge(int age) throws Exception {",
"        if (age < 18)",
"            throw new Exception(\"Not eligible — age is \" + age);   // 'throw'",
"        System.out.println(\"Eligible to vote\");",
"    }",
"",
"    public static void main(String[] args) {",
"        int[] arr = { 10, 20, 30 };",
"",
"        try {",
"            System.out.println(arr[1] / 0);          // ArithmeticException",
"            System.out.println(arr[5]);              // never reached",
"        }",
"        catch (ArithmeticException e) {              // most specific first",
"            System.out.println(\"Arithmetic: \" + e.getMessage());",
"        }",
"        catch (ArrayIndexOutOfBoundsException e) {",
"            System.out.println(\"Bad index: \" + e.getMessage());",
"        }",
"        catch (Exception e) {                        // most general LAST",
"            System.out.println(\"General: \" + e);",
"        }",
"        finally {",
"            System.out.println(\"finally always runs — closing resources\");",
"        }",
"",
"        try { checkAge(15); }",
"        catch (Exception e) { System.out.println(\"Caught: \" + e.getMessage()); }",
"",
"        System.out.println(\"Program continues normally\");",
"    }",
"}",
"```",
"",
"**Output:**",
"```",
"Arithmetic: / by zero",
"finally always runs — closing resources",
"Caught: Not eligible — age is 15",
"Program continues normally",
"```",
"",
"> **Rule:** catch blocks must go from **most specific to most general**. Writing `catch (Exception e)` before `catch (ArithmeticException e)` is a compile error, because the second is unreachable.",
"",
"---",
"",
"#### Advantages of exception handling",
"",
"1. **Separates error-handling code from normal logic** — the algorithm stays readable instead of being buried under error checks.",
"2. **The program does not crash** — execution continues after the handler.",
"3. **Errors propagate up the call stack** — a method that cannot deal with a problem passes it to one that can, instead of returning meaningless error codes.",
"4. **Errors are grouped and typed** — a hierarchy of exception classes lets you catch broadly or narrowly.",
"5. **Meaningful diagnostics** — message, type and full stack trace.",
"6. **Guaranteed cleanup** — `finally` and try-with-resources always release files, sockets and connections.",
"7. **Enforced handling** for checked exceptions — the compiler will not let you silently ignore an I/O failure.",
"",
"---",
"",
"#### User-defined exceptions",
"",
"Create your own by extending `Exception` (checked) or `RuntimeException` (unchecked).",
"",
"**Question asked in Nov 2025 — \"No Match Exception\":**",
"",
"```java",
"import java.util.Scanner;",
"",
"// Step 1 — define the exception",
"class NoMatchException extends Exception {",
"    public NoMatchException(String message) {",
"        super(message);                  // pass the message to Exception",
"    }",
"}",
"",
"public class NoMatchDemo {",
"",
"    // Step 2 — a method that throws it",
"    static void checkCountry(String input) throws NoMatchException {",
"        if (!input.equals(\"India\"))",
"            throw new NoMatchException(\"No Match Exception: \\\"\" + input + \"\\\" is not India\");",
"        System.out.println(\"Match found — welcome to India!\");",
"    }",
"",
"    public static void main(String[] args) {",
"        Scanner sc = new Scanner(System.in);",
"        System.out.print(\"Enter a country name: \");",
"        String country = sc.nextLine();",
"",
"        // Step 3 — handle it",
"        try {",
"            checkCountry(country);",
"        } catch (NoMatchException e) {",
"            System.out.println(e.getMessage());",
"        } finally {",
"            sc.close();",
"        }",
"    }",
"}",
"```",
"",
"**Sample runs:**",
"```",
"Enter a country name: India",
"Match found — welcome to India!",
"",
"Enter a country name: Nepal",
"No Match Exception: \"Nepal\" is not India",
"```",
"",
"---",
"",
"#### Exception propagation",
"",
"If a method does not catch an exception, it is **passed up the call stack** to its caller, then to that method's caller, and so on. If it reaches `main` uncaught, the JVM's default handler prints the stack trace and terminates the program.",
"",
"```java",
"class Propagate {",
"    void m() { int x = 10 / 0; }          // exception originates here",
"    void n() { m(); }                     // does not catch — propagates",
"    void p() {",
"        try { n(); }                      // caught three levels up",
"        catch (ArithmeticException e) { System.out.println(\"Handled in p(): \" + e); }",
"    }",
"    public static void main(String[] a) { new Propagate().p(); }",
"}",
"```",
"",
"**Rule:** *unchecked* exceptions propagate automatically. *Checked* exceptions propagate only if every method in the chain declares them with `throws`.",
"",
"---",
"",
"#### Preventing certain exception types from being thrown",
"",
"A method can restrict what it propagates by catching broadly and re-throwing only what it permits:",
"",
"```java",
"class RestrictedException extends RuntimeException {",
"    RestrictedException(String m) { super(m); }",
"}",
"",
"public class Restrict {",
"    // This method is not allowed to leak ArithmeticException to its caller.",
"    static int safeDivide(int a, int b) {",
"        try {",
"            return a / b;",
"        } catch (ArithmeticException e) {",
"            throw new RestrictedException(\"Division by zero is not permitted here\");",
"        }",
"    }",
"",
"    public static void main(String[] args) {",
"        try { System.out.println(safeDivide(10, 0)); }",
"        catch (RestrictedException e) { System.out.println(e.getMessage()); }",
"    }",
"}",
"```",
"",
"An overriding method also **cannot** throw a broader checked exception than the method it overrides — the compiler enforces this."
  ].join("\n");

  var MAP = {
    // Module 1
    "oop-2022-q1a": "oop-vs-pop", "oop-2022-q2a": "oop-vs-pop",
    "oop-2023-q1a": "oop-vs-pop", "oop-2023-q1b": "oop-vs-pop",
    "oop-2024-q1a": "oop-vs-pop",
    "oop-2022-q1b": "oop-concepts", "oop-2022-q2b": "oop-concepts",
    "oop-2023-q2a": "oop-concepts", "oop-2023-q2b": "oop-concepts",
    "oop-2025-q1b": "oop-concepts", "oop-2025-q1c": "oop-concepts",
    "oop-2024-q1c": "java-basics", "oop-2025-q1a": "java-basics",
    "oop-2024-q1b": "access-modifiers",
    // Module 2
    "oop-2022-q3a": "constructors", "oop-2023-q3a": "constructors",
    "oop-2024-q2b": "constructors", "oop-2025-q2b": "constructors",
    "oop-2022-q3b": "access-modifiers",
    "oop-2022-q4a": "encapsulation-abstraction", "oop-2023-q3b": "encapsulation-abstraction",
    "oop-2024-q2a": "encapsulation-abstraction", "oop-2025-q2a": "encapsulation-abstraction",
    "oop-2022-q4b": "classes-objects", "oop-2024-q2c": "classes-objects",
    "oop-2025-q2c": "classes-objects",
    "oop-2023-q4a": "message-passing",
    "oop-2023-q4b": "static-members",
    // Module 3
    "oop-2022-q5a": "abstract-interface", "oop-2022-q6b": "abstract-interface",
    "oop-2024-q3b": "abstract-interface", "oop-2024-q3c": "abstract-interface",
    "oop-2025-q3c": "abstract-interface",
    "oop-2022-q5b": "association-aggregation", "oop-2023-q5a": "association-aggregation",
    "oop-2023-q5b": "association-aggregation", "oop-2023-q6a": "association-aggregation",
    "oop-2025-q3b": "association-aggregation",
    "oop-2022-q6a": "inheritance", "oop-2023-q6b": "inheritance",
    "oop-2024-q3a": "inheritance", "oop-2025-q3a": "inheritance",
    // Module 4
    "oop-2022-q7a": "polymorphism", "oop-2022-q7b": "polymorphism",
    "oop-2022-q8a": "polymorphism", "oop-2022-q8b": "polymorphism",
    "oop-2023-q7a": "polymorphism", "oop-2023-q7b": "polymorphism",
    "oop-2023-q8a": "polymorphism", "oop-2023-q8b": "polymorphism",
    "oop-2024-q4a": "polymorphism", "oop-2024-q4b": "polymorphism",
    "oop-2024-q4c": "polymorphism", "oop-2025-q4a": "polymorphism",
    "oop-2025-q4b": "polymorphism", "oop-2025-q4c": "polymorphism",
    // Module 5
    "oop-2022-q9a": "multithreading", "oop-2022-q10b": "multithreading",
    "oop-2023-q9b": "multithreading", "oop-2023-q10a": "multithreading",
    "oop-2023-q10b": "multithreading", "oop-2024-q5a": "multithreading",
    "oop-2025-q5a": "multithreading",
    "oop-2022-q9b": "exception-handling", "oop-2022-q10a": "exception-handling",
    "oop-2023-q9a": "exception-handling", "oop-2024-q5b": "exception-handling",
    "oop-2024-q5c": "exception-handling", "oop-2025-q5b": "exception-handling",
    "oop-2025-q5c": "exception-handling"
  };

  Object.keys(MAP).forEach(function (qid) { window.SOLUTIONS[qid] = S[MAP[qid]]; });
})();
